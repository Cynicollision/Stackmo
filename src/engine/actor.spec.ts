import { Actor, ActorInstance } from './actor';

describe('Actor', () => {

    let testActor;

    beforeEach(() => {
        testActor = new Actor();
    });

    it('needs tests', () => {
        console.log('needs tests');

        expect(true).toBe(true);
    });

});
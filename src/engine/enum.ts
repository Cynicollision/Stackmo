export enum ActorState {
    Active = 1,
    Destroyed = 2,
}

export enum Direction {
    Right = 0,
    Down = 90,
    Left = 180,
    Up = 270,
}

export enum GameState {
    Running = 1,
    Paused = 2,
}
